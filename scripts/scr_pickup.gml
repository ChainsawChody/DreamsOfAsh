///scr_pickup
//Set Item Name
//var i = instance_create(x, y - 25, obj_pickup);
//i.str = string(itemname);




/*
if(instance_number(obj_pickup) > 1)
{
    yy = y;
    if(y == yy)
    {
        y += 15;
        instance_create(x, y, obj_pickup)
    }
}
instance_destroy();

test

